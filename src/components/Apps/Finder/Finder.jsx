import React from "react";
import ResizableWindow from "../ResizableWindow";

const Finder = React.memo(() => {
  return <ResizableWindow appName="finder"><div className="m-10">finder</div></ResizableWindow>;
});

export default Finder;
 